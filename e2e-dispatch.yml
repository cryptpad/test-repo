- name: Repository Dispatch
  uses: cryptpad/test-repo
  with:
    token: ${{ secrets.TEST_PAT }}
    repository: cryptpad/e2e-test-suite
    event-type: trigger-e2e-tests
    # client-payload: '{"ref": "${{ github.ref }}", "sha": "${{ github.sha }}"}'